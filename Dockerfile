# =============================================================
# TodoList 根目录 Dockerfile（说明文件）
# 实际构建：
#   后端镜像 → ./backend/Dockerfile
#   前端镜像 → ./frontend/Dockerfile
# 生产部署使用 docker-compose.prod.yml
# =============================================================

# 此文件仅作说明，实际镜像由 backend/Dockerfile 和 frontend/Dockerfile 分别定义
# GitHub Actions CI/CD 会自动调用子目录 Dockerfile 进行多阶段构建
