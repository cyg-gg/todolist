# =============================================================
# TodoList 项目根目录 Dockerfile（多阶段构建，仅用于本地/CI测试）
# 生产部署请使用 docker-compose.yml 分别构建前后端镜像
# =============================================================

# 此文件仅作标识，实际镜像由 backend/Dockerfile 和 frontend/Dockerfile 分别定义
# docker-compose.yml 会自动引用两个子 Dockerfile
